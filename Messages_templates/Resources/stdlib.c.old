/*1.運行console指令（參數只適用部份系統）
 版本：1.00(0) */
	system("");
	/*for windows series console
		cls→clear console
		pause→get any char from keyboard and continue
	*/
	/*for unix series console
		clear→clear console
		ls→same as dir in windows
	*/

/*malloc
  版本：1.11(2)*/
a
    printf("正在向系統要求一段記憶體空間...\n"
           "Requesting a memory space from the operating system...");
           
    * memPtr = ( *) malloc(sizeof() * );

    if(memPtr != NULL){
      printf("成功！\n");
    }else{
      printf("失敗！請確定可用記憶體是否不足。\n");
    }
b

/*free
  版本：1.01(1)*/
a
    free(allocPtr);
    allocPtr = NULL;
b

